# План улучшения интерфейса GigaAM

## Текущее состояние

Приложение представляет собой **консольный CLI** для распознавания речи в реальном времени.  
Текущий интерфейс:
- Текстовый вывод в консоль с emoji-индикаторами
- Полосы уровня звука (`▓▓▓░░░░░░░`)
- Режимы: непрерывный и push-to-talk
- Отсутствует GUI

---

## 1. Графический интерфейс (GUI)

### 1.1 Переход на PyQt6 / Tkinter

| Компонент | Описание |
|-----------|----------|
| **Главное окно** | Минимальное окно с тремя зонами: заголовок, область текста, панель управления |
| **Область транскрипции** | Прокручиваемый текстовый виджет с подсветкой нового текста |
| **Панель статуса** | Индикатор уровня звука, статус записи, выбранное устройство |

### 1.2 Компоненты интерфейса

```
┌────────────────────────────────────────┐
│  🎤 GigaAM Realtime ASR         [—][×] │
├────────────────────────────────────────┤
│  ┌─ Устройство ──────────────────────┐ │
│  │  [▾ Microphone (Realtek Audio)]   │ │
│  └───────────────────────────────────┘ │
│                                        │
│  ┌─ Транскрипция ────────────────────┐ │
│  │                                   │ │
│  │  Привет, это тестовое сообщение   │ │
│  │  для распознавания речи...        │ │
│  │                                   │ │
│  │                           ▾▾▾▾▾▾▾ │ │
│  └───────────────────────────────────┘ │
│                                        │
│  [▓▓▓▓▓░░░░░] 0.42   ● REC   ⏸ Пауза  │
│                                        │
│  [🎙 Запись]  [📋 Копировать]  [⚙]    │
└────────────────────────────────────────┘
```

---

## 2. Улучшение UX консольного режима

### 2.1 Интерактивное меню выбора устройства

```python
# При запуске без --device показывать интерактивный список:
# [1] Microphone (Realtek Audio)
# [2] Stereo Mix (Realtek Audio)  [LOOPBACK]
# [3] VoiceMeeter Output
# Введите номер устройства: _
```

### 2.2 Цветной вывод (colorama / rich)

| Элемент | Цвет |
|---------|------|
| Статус записи | 🔴 Красный (запись), 🟢 Зелёный (готов) |
| Ошибки | 🟡 Жёлтый / 🔴 Красный |
| Транскрипция | Белый с подсветкой новых слов |
| Уровень звука | Градиент от зелёного к красному |

### 2.3 Rich Live Display

Использование `rich.live` для динамического обновления консоли без мерцания:

```python
from rich.live import Live
from rich.panel import Panel
from rich.progress import Progress

with Live(generate_panel(), refresh_per_second=10):
    # Обновление в реальном времени
```

---

## 3. Системный трей (System Tray)

### 3.1 Минимизация в трей

- Приложение сворачивается в системный трей
- Иконка меняется в зависимости от состояния:
  - 🎤 Ожидание
  - 🔴 Запись
  - ⏸ Пауза

### 3.2 Контекстное меню трея

```
┌─────────────────────────┐
│  🎤 Начать запись       │
│  ⏹ Остановить           │
│  ────────────────────── │
│  📋 Последняя фраза     │
│  📂 Открыть лог         │
│  ────────────────────── │
│  ⚙ Настройки            │
│  ❌ Выход               │
└─────────────────────────┘
```

---

## 4. Горячие клавиши (Hotkeys)

### 4.1 Глобальные горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl + Shift + R` | Начать/остановить запись |
| `Ctrl + Shift + P` | Пауза/продолжить |
| `Ctrl + Shift + C` | Копировать последнюю фразу |
| `Ctrl + Shift + H` | Показать/скрыть окно |

### 4.2 Настраиваемые клавиши

Конфигурационный файл `~/.gigaam/config.json`:

```json
{
  "hotkeys": {
    "toggle_recording": "ctrl+shift+r",
    "push_to_talk": "space",
    "copy_last": "ctrl+shift+c"
  }
}
```

---

## 5. Файл конфигурации

### 5.1 Структура конфига

```json
{
  "device": {
    "id": null,
    "name": "Microphone",
    "use_loopback": false
  },
  "asr": {
    "model": "gigaam-v3-e2e-rnnt",
    "buffer_seconds": 3.0,
    "vad_threshold": 0.01
  },
  "ui": {
    "theme": "dark",
    "font_size": 14,
    "minimize_to_tray": true,
    "show_audio_level": true
  },
  "output": {
    "auto_copy": true,
    "save_to_file": false,
    "file_path": "./transcripts/"
  },
  "hotkeys": {
    "toggle_recording": "ctrl+shift+r",
    "push_to_talk": "space"
  }
}
```

### 5.2 GUI настроек

Диалоговое окно с вкладками:
1. **Аудио** — выбор устройства, порог VAD
2. **Модель** — выбор модели, размер буфера
3. **Интерфейс** — тема, шрифт, поведение трея
4. **Горячие клавиши** — настройка комбинаций
5. **Вывод** — автокопирование, сохранение в файл

---

## 6. Улучшения отображения текста

### 6.1 Подсветка новых слов

Новые распознанные слова выделяются:
- Подсветка фона на 1 секунду
- Плавное затухание анимации

### 6.2 Временные метки

```
[14:32:05] Привет, как дела?
[14:32:08] Сегодня хорошая погода.
```

### 6.3 Сегментация по паузам

Автоматическое разбиение на абзацы при паузе > 2 секунд.

---

## 7. Индикаторы и визуализация

### 7.1 Улучшенный VU-метр

```
Слабый   ████░░░░░░░░░░░░░░░░  20%
Норма    ████████████░░░░░░░░  60%
Громко   ████████████████████  100% 🔴
```

### 7.2 Спектрограмма (опционально)

Мини-визуализация спектра аудио для продвинутых пользователей.

---

## 8. Уведомления

### 8.1 Push-уведомления (toast)

| Событие | Уведомление |
|---------|-------------|
| Запись начата | «🎤 Запись начата» |
| Текст скопирован | «📋 Текст в буфере обмена» |
| Ошибка устройства | «⚠️ Микрофон отключён» |

### 8.2 Звуковые сигналы

- Короткий "бип" при начале записи
- Двойной "бип" при остановке

---

## 9. Режимы работы

### 9.1 Компактный режим

Минимальное окно только с кнопкой записи и последней фразой:

```
┌─────────────────────────────┐
│ 🔴 Последняя фраза здесь... │
└─────────────────────────────┘
```

### 9.2 Overlay режим

Полупрозрачное окно поверх всех окон для субтитров:

```
┌─────────────────────────────────────────┐
│         Текст распознавания речи        │ ← Полупрозрачный фон
└─────────────────────────────────────────┘
```

---

## 10. План реализации

### Фаза 1: Улучшение CLI (1-2 дня) ✅ ЗАВЕРШЕНО
- [x] Интеграция `rich` для красивого вывода
- [x] Интерактивный выбор устройства (`--interactive`)
- [x] Цветовые индикаторы (градиент 🟢→🟡→🔴)
- [x] Live Display без мерцания
- [x] Fallback для систем без Rich (`--no-rich`)

### Фаза 2: Базовый GUI (3-5 дней)
- [ ] Создание PyQt6 окна
- [ ] Реализация основных виджетов
- [ ] Интеграция с RealtimeASR

### Фаза 3: Системный трей (1-2 дня)
- [ ] Добавление иконки в трей
- [ ] Контекстное меню
- [ ] Глобальные горячие клавиши

### Фаза 4: Настройки (2-3 дня)
- [x] Файл конфигурации (`src/config.py`)
- [ ] GUI настроек
- [ ] Персистентность состояния

### Фаза 5: Дополнительные режимы (2-3 дня)
- [ ] Компактный режим
- [ ] Overlay для субтитров
- [ ] Push-уведомления

---

## 11. Созданные файлы

### Фаза 1 (10.12.2024)

| Файл | Описание |
|------|----------|
| `src/ui/__init__.py` | Пакет UI компонентов |
| `src/ui/console.py` | Rich Console UI: `RichConsoleUI`, `DeviceSelector`, `SimpleConsoleUI` |
| `src/config.py` | Управление конфигурацией: dataclass-структуры, JSON сериализация |

### Обновлённые файлы

| Файл | Изменения |
|------|-----------|
| `requirements.txt` | Добавлены: `rich`, `pystray`, `plyer`, `pynput` |
| `run_live.py` | Интеграция Rich UI, новые аргументы `--interactive`, `--no-rich` |

---

## Зависимости для GUI

```txt
# requirements.txt дополнения
PyQt6>=6.5.0          # GUI фреймворк (опционально)
rich>=13.0.0          # Улучшенный консольный вывод ✅
pystray>=0.19.0       # Системный трей ✅
plyer>=2.1.0          # Кроссплатформенные уведомления ✅
pynput>=1.7.6         # Глобальные горячие клавиши ✅
```

---

## Приоритеты

1. **✅ Критично**: Улучшение CLI с `rich` — ЗАВЕРШЕНО
2. **🟠 Высокий**: Базовый GUI с выбором устройства
3. **🟡 Средний**: Системный трей и горячие клавиши
4. **🟢 Низкий**: Overlay режим и спектрограмма

---

## Использование новых функций

### Интерактивный выбор устройства
```bash
python run_live.py --interactive
```

### Список устройств (Rich таблица)
```bash
python run_live.py --list-devices
```

### Отключить Rich UI
```bash
python run_live.py --no-rich
```

